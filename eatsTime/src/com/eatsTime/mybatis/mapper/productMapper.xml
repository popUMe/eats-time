<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="product">
   	<insert id="insert">
   		insert into tbl_product
		(member_id, product_category, product_name, product_price, product_date, product_expiration_date, product_stock, product_address, product_addres_detail)
		values(#{memberId},#{productCategory},#{productName},#{productPrice},#{productDate}, #{productExpirationDate},#{productStock},#{productAddress},#{productAddresDetail})
   </insert>
</mapper>

